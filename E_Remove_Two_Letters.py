n = int(input())
for ind in range(n):
    from collections import defaultdict
    hmap = defaultdict(int)
    sz = int(input())
    val = input()
    for ind in range(sz-1):
        hmap[val[ind:ind+2]] +=1
    
    ans = 0
    for ind in range(len()):
        
    print(hmap)
    
    # for ind in range(sz-1):
